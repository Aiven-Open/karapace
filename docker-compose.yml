services:
  kafka:
    image: apache/kafka:4.1.1
    ports:
      - "9094:9094"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: INTERNAL://:9092,EXTERNAL://:9094,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:9092,EXTERNAL://localhost:9094
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --list"]
      interval: 10s
      timeout: 10s
      retries: 5

#  karapace:
#    image: karapace:latest-with-hook
#    ports:
#      - "8081:8081"
#    depends_on:
#      kafka:
#        condition: service_healthy
#    volumes:
#      # Host: mkdir -p karapace-hook
#      #   socat UNIX-LISTEN:karapace-hook/schema-hook.sock,unlink-early,umask=000,fork SYSTEM:'tee /dev/stderr'
#      # tee copies each payload to stderr (your terminal) and to stdout (back to Karapace). Use /dev/tty if stderr is redirected.
#      # umask=000 → socket mode 666 so the image `karapace` user (not your host UID) can connect.
#      # :z relabels the mount for container access (needed on Fedora/RHEL with SELinux).
#      # If it still fails: `chmod 666 karapace-hook/schema-hook.sock` after socat starts, or set user: "0:0" on this service (dev only).
#      - ./karapace-hook:/run/karapace-hook:z
#    environment:
##      KARAPACE_LOG_LEVEL: INFO
#      KARAPACE_BOOTSTRAP_URI: kafka:9092
#      KARAPACE_PORT: 8081
#      KARAPACE_HOST: 0.0.0.0
#      KARAPACE_REPLICATION_FACTOR: 1
#      KARAPACE_TOPIC_NAME: _schemas
#      KARAPACE_HOOK_SOCKET: /run/karapace-hook/schema-hook.sock
#    entrypoint:
#      - python3
#      - -m
#      - karapace
#    command: registry
#    healthcheck:
#      test: ["CMD", "curl", "-f", "http://localhost:8081/"]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#      start_period: 30s
